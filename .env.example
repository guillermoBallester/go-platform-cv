# Application
# Set to "development" to enable local defaults, "production" for strict validation
APP_ENV=development

# Server
PORT=8080
SERVER_READ_TIMEOUT=10s
SERVER_WRITE_TIMEOUT=30s

# Database - Option 1: Full URL (recommended for cloud deployments)
DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=disable

# Database - Option 2: Individual components
# Use this approach when you need fine-grained control over connection parameters
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=gocv
# DB_SSLMODE=disable

# Connection Pool Settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m
